/*
 * d) Apresente o comando para imprimir na tela o valor-chave do nó raiz 7 duas vezes. 
 *    Em uma impressão, utilize a variável "raiz" deve ser utilizada. Na outra, use a variável "r".
 */

 printf("%d %d", raiz->chave, (*r)->chave);